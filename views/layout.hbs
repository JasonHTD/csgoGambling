<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>CSGO Gambling | {{title}}</title>
    {{#each cssFiles}}
    <link rel="stylesheet" href="{{this}}">
    {{/each}}
    {{#each jsFiles}}
      <script src="{{this}}" charset="utf-8"></script>
    {{/each}}
  </head>
  <body>
    {{{body}}}
  </body>
</html>
