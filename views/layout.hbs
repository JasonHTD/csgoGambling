<!DOCTYPE html>
<html>
  <head>
    <link href="https://fonts.googleapis.com/css?family=Raleway:500,900" rel="stylesheet">
    <meta charset="utf-8">
    <title>G4K | {{title}}</title>
    {{#each cssFiles}}
    <link rel="stylesheet" href="{{this}}">
    {{/each}}
    {{#each jsFiles}}
      <script src="{{this}}" charset="utf-8"></script>
    {{/each}}
  </head>
  <body>
    {{>navbar}}
    {{{body}}}
  </body>
</html>
